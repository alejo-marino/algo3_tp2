package edu.fiuba.algo3.modelo;

import edu.fiuba.algo3.Modelo.excepciones.ReforzarPaisAjenoError;
import edu.fiuba.algo3.Modelo.excepciones.TurnoInvalidoError;

import java.util.ArrayList;

public class Turno {
    /*
    public ArrayList<Jugador> listaJugadores;
    public Jugador jugadorActual;
    private Juego juegoActual;

    public Turno(ArrayList<Jugador> listaJugadores, Juego juego) {
        this.listaJugadores = listaJugadores;
        this.juegoActual = juego;
        jugadorActual = listaJugadores.get(0);
    }

    public void realizarMovimiento(Jugador jugador) {
        if (jugador != jugadorActual) {
            throw new TurnoInvalidoError("No es su turno.");
        }
    }

    public void agrupar(Jugador jugador, Pais pais, int cantidadTropas) {
        realizarMovimiento(jugador);
        juegoActual.agruparA(jugador, pais, cantidadTropas);
    }

     */
}
